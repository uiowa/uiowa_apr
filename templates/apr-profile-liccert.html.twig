{#
/**
 * @file
 * Default theme implementation for the APR Profile pseudo-fields.
 *
 * Available variables
 * - data: Array of licensures and certifications information from liccert[] array.
 */
#}

{% if data is not empty %}

<div class="field field-name-apr-profile-liccert"{{ attributes }}>
  <div class="field-label">Licensures and Certifications</div>
  <ul class="field-items">
    {% for key, item in data %}
      <li class="field-item {{ loop.index is odd ? 'odd' : 'even' }}">
        {% if item.formatted is not empty %}
          {{ item.formatted|raw }}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
</div>

{% endif %}
