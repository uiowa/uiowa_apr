{#
/**
 * @file
 * Default theme implementation for the APR Profile pseudo-fields.
 *
 * Available variables
 * - data: Array of creative works information from creative_works[] array.
 */
#}

{% if data is not empty %}

<div class="field field-name-apr-profile-creative-works"{{ attributes }}>
  <h2 class="field-header">Creative Works</h2>
  <ul class="field-items">
    {% for key, item in data %}
      <li class="field-item {{ loop.index is odd ? 'odd' : 'even' }}">
        {% if item.formatted is not empty %}
          {{ item.formatted|raw }}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
</div>

{% endif %}
