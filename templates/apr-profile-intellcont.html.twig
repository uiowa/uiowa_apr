{#
/**
 * @file
 * Default theme implementation for the APR Profile pseudo-fields.
 *
 * Available variables
 * - data: Array of publication information from intellcont[] array.
 */
#}

{% if data is not empty %}

<div class="field field-name-apr-profile-intellcont"{{ attributes }}>
  <div class="field-label">Publications</div>
  <ul class="field-items">
    {% for key, item in data %}
      <li class="field-item {{ loop.index is odd ? 'odd' : 'even' }}">
        {% if item.formatted is not empty %}
          {{ item.formatted|raw }}
        {% endif %}
      </li>
    {% endfor %}
  </ul>
</div>

{% endif %}
